# 홈페이지에서 콘텐츠 관리하기

GitHub Pages는 홈페이지 코드를 배포하고, Supabase는 사진·문서·게시글·회원 정보를 저장합니다.
이번 코드를 한 번 배포한 뒤에는 일반적인 콘텐츠 수정에 Git 푸시가 필요 없습니다.

## 지금 한 번만 할 일

1. 이미 대표 사진 업로드가 작동하는 프로젝트의 **SQL Editor → New query**를 엽니다.
2. `supabase/003-community.sql` 전체를 붙여넣고 **Run** 합니다. 기존 대표 사진과 관리자 계정은 유지됩니다.
3. `http://localhost:8741/admin.html`에서 새로고침합니다. **홈페이지 콘텐츠 → 콘텐츠 관리 열기**로 이동합니다.
4. 운영진 소개나 테스트 앨범을 등록해 공개 페이지에서 확인합니다.
5. 확인한 코드를 GitHub Pages에 배포합니다. 배포 전에는 새 기능이 로컬 주소에서만 보입니다.

이번 단계에서는 001이나 첫 관리자 지정 SQL을 다시 실행할 필요가 없습니다.
새 Supabase 프로젝트를 처음부터 구성할 때만 001 → 003 → 004 순서로 실행하고, Google 로그인 후 002로 첫 관리자를 지정합니다.
기존 사이트에서 001을 재실행했다면 003 → 004도 뒤이어 실행해 새 권한 정책을 복원합니다. 003을 재실행한 경우에도 004를 마지막에 실행합니다.

## 운영진·지도교수 소개

**콘텐츠 관리 → 운영진 소개 → 추가/수정**에서 이름, 직함, 소속, 소개, 공개 이메일, 링크, 사진을 관리합니다.
사진을 등록하지 않거나 **기존 사진 비우기**를 선택하면 사진 영역은 비워 둡니다.
표시 순서는 작은 숫자부터 정렬됩니다. **홈페이지에 공개**를 해제하면 소개를 숨깁니다.

이 소개의 직함과 로그인 권한은 별개입니다. 소개에 ‘회장’을 입력한다고 관리자 권한이 생기지 않습니다.

## 명단·레벨 및 회원 승인

1. **명단·레벨 → 추가**에서 회원을 등록합니다. 이름·트랙·레벨·점수·최근 승급일은 공개됩니다. 내부 메모와 계정 연결 정보는 공개되지 않습니다.
2. 회원이 자신의 Google 계정으로 홈페이지에 한 번 로그인합니다.
3. **회원 승인**에서 그 계정을 선택하고 명단과 연결한 뒤 **동아리원 승인**으로 저장합니다.
4. 회원이 새로고침하면 게시글과 승급자료를 작성할 수 있습니다.

엑셀 명단은 **명단 일괄 등록**에서 이름 / 트랙 / 레벨 / 점수 네 열을 복사·붙여넣기로 최대 200명씩 등록할 수 있습니다.
헤더는 넣지 않습니다. 이름·트랙 중복 또는 잘못된 값이 있으면 해당 일괄 등록 전체를 취소하므로 일부만 등록되지 않습니다.
같은 이름과 트랙을 가진 서로 다른 사람은 일반 **추가**로 구분해 등록하고 Google 계정 연결을 확인합니다.

레벨을 직접 정정할 때에는 수정 사유를 입력합니다. 변경 전·후 값과 처리자, 사유가 **변경 이력**에 남습니다.
**활동 중인 회원**을 해제하면 공개 명단에서 숨겨지고 해당 일반 회원의 글 작성이 제한됩니다.
다른 계정을 **운영진 지정**으로 변경하면 전체 콘텐츠와 회원 권한, 승급 심사를 관리할 수 있습니다.
자기 자신의 운영진 권한은 이 화면에서 해제할 수 없고, 권한 변경은 서버의 `account_events`에 남습니다.

## 갤러리

**갤러리 → 앨범 추가 → 사진·영상 관리 → 사진 추가** 순서로 사용합니다.
원본 장당 15MB 이하, 한 번에 최대 20장을 선택합니다. 긴 변 최대 1800px의 WebP로 압축하고 저장 파일은 5MB 이하로 제한합니다.
사진·영상 설명과 표시 순서를 바꿀 수 있습니다. 첫 번째 표시 항목을 앨범 표지로 사용합니다.
업로드 중 일부 실패하면 이미 저장된 장수를 안내하므로 나머지만 다시 선택합니다.

### 동영상 기능을 처음 사용할 때

1. Supabase **SQL Editor → New query**에서 `supabase/004-gallery-video.sql` 전체를 실행합니다. 기존 사진·앨범은 유지됩니다.
2. 새 코드를 배포하고 관리 화면을 새로고침합니다. 이전 001·003 SQL은 다시 실행하지 않아도 됩니다.
3. **사진·영상 관리 → 동영상 업로드**에서 MP4 또는 WebM을 한 번에 한 개 등록합니다. 파일당 50MB 이하이며, 서버에서 자동 압축·변환하지 않습니다.
4. **유튜브 영상 추가**는 영상 주소와 설명만 저장합니다. 일반 영상·Shorts·공유·라이브 주소를 지원하며 방문자가 앨범 안에서 플레이어를 열어 재생할 수 있습니다.

직접 올린 영상은 재생 버튼을 누르기 전 원본을 미리 내려받지 않으며, 앨범 표지에는 업로드 시 만든 정지 이미지를 표시합니다. 호환성이 필요한 영상은 MP4(H.264 영상·AAC 음성)를 사용하세요. MOV 파일은 변환하거나 유튜브에 올린 뒤 링크를 등록합니다. 비공개 영상이나 외부 재생을 금지한 유튜브 영상은 재생할 수 없습니다.

큰 영상·긴 영상은 유튜브 링크를 권장합니다. 직접 업로드에는 이어 올리기가 없으며 연결이 끊기면 재시도해야 합니다. 업로드가 용량 제한으로 실패하면 Supabase Storage 설정의 프로젝트 전체 파일 크기 제한도 확인하세요. 버킷 제한은 프로젝트 전체 제한을 넘을 수 없습니다. [Supabase 파일 제한](https://supabase.com/docs/guides/storage/uploads/file-limits)

운영진·갤러리 이미지와 직접 올린 영상은 공개 저장소입니다. ‘숨김’은 홈페이지 목록에서 제외하는 기능이며, 이미 알려진 파일 URL 자체를 회수하지는 않습니다.

## 게시판·승급자료

- 승인된 회원과 운영진이 작성할 수 있습니다. 공지는 운영진만 작성합니다.
- **글 작성 → 작성 중으로 저장 → 파일 첨부 → 게시글 공개** 순서입니다.
- 일반 글은 작성자 또는 운영진이 작성 중으로 돌려 수정하거나 보관할 수 있습니다. 보관함에서 다시 작성 중으로 복원할 수 있습니다.
- 공개된 본문·링크·첨부자료는 방문자 누구나 볼 수 있습니다. **승급자료도 공개**합니다.
- 작성 중인 글과 첨부파일은 작성자·운영진만 볼 수 있습니다.
- 파일은 PDF, DOCX, PPTX, XLSX, TXT, ZIP, JPG, PNG, WebP를 지원합니다. 글당 최대 10개, 파일당 25MB입니다.
- 영상 원본은 첨부하지 않고 유튜브 링크로 등록합니다.
- 문서는 비공개 Storage 버킷에 보관하고, 서버가 해당 글의 공개·작성자 권한을 확인한 뒤 60초 다운로드 링크를 발급합니다. 링크가 발급된 뒤 공개를 해제하면 이미 발급된 링크는 만료 전까지 사용할 수 있습니다.

## 승급 처리

1. 본인 Google 계정을 활동 중인 회원 명단과 연결합니다. 운영진도 본인 신청에는 연결이 필요합니다.
2. **승급자료** 분류로 글을 만들고 세미나 발표 / 자료 문서화 / 영상 발표 중 유형을 선택합니다.
3. 문서·사진 파일 또는 자료·영상 링크를 최소 하나 첨부합니다. 신청 레벨은 현재 트랙의 다음 단계로 정해집니다.
4. **승급자료 공개·심사 요청**을 누르면 방문자에게 공개되고 심사 대기가 됩니다. 심사 중에는 자료를 수정할 수 없습니다.
5. 다른 운영진이 **승급 심사**에서 자료를 확인하고 의견과 함께 승인 또는 보완 요청을 저장합니다.
6. 승인하면 레벨과 최근 승급일, 심사 기록이 한 번의 DB 트랜잭션에서 함께 반영됩니다. 점수는 자동 가산하지 않습니다.
7. 보완 요청을 받으면 작성자가 글을 작성 중으로 돌려 수정한 뒤 다시 제출합니다. 심사 당시의 본문·링크·첨부파일과 의견은 본인·운영진에게 이력으로 남습니다.

운영 트랙 LV1~3, 개발 트랙 LV1~4를 사용합니다. 한 회원의 심사 대기 신청은 한 건만 허용합니다.
본인 승인은 막았으므로, 첫 운영진이 직접 승급을 신청하려면 다른 운영진 계정을 지정해야 합니다.
심사 도중 회원 레벨이 정정되면 자동 승인을 중단하고 보완 요청·재신청으로 처리합니다.

## 이전 브라우저의 임시 기록

기존 localStorage 기록은 삭제하거나 자동 공개하지 않습니다.
**명단·레벨 → 이 브라우저의 이전 기록 내려받기**로 명단·앨범 정보·게시글을 JSON으로 보관할 수 있습니다.
다른 브라우저 또는 다른 주소에 저장한 기록은 그 브라우저·주소에서 내려받아야 합니다.
이전의 예시 데이터는 실제 회원·활동으로 서버에 자동 등록하지 않습니다. 필요한 명단은 일괄 등록하고, 실제 글과 앨범은 확인 후 등록하세요.
기존 사진 자리(image-slot)에서만 표시되던 사진은 서버 파일이 아니므로 원본 사진을 다시 선택해야 합니다.

## 운영·검증

- SQL 권한·승급 테스트: `npm test` (실제 PostgreSQL 엔진인 PGlite 사용, Supabase 기본 역할·Storage 구조를 최소 재현).
- 페이지 연결·문법 검사: `node scripts/check-community.mjs`.
- 실제 프로젝트 읽기 전용 확인: `node scripts/check-supabase.mjs`.
- 실제 Supabase에서는 운영진/일반 회원/로그아웃 상태로 공개 범위와 파일 다운로드를 확인하고, 두 운영진의 동시 심사가 한 번만 반영되는지 확인합니다.
- 게시판·앨범은 페이지 단위 조회와 이미지 지연 로딩을 사용합니다. 관리 목록은 소규모 동아리 기준이며 계정 최대 1,000개·게시글 최근 500개를 표시합니다.
- 업로드 후 연결에 실패한 파일, 교체 전 사진, 심사 이력용 문서는 자동 삭제하지 않습니다. 참조와 심사 스냅샷을 확인한 뒤 별도 정리가 필요합니다.
- DB 백업과 Storage 파일 백업은 별도로 보관해야 합니다. 이번 변경에는 자동 백업·정리 작업을 추가하지 않았습니다.
- `notion-proxy`와 이전 Notion 안내서는 참고용으로 남아 있으며, 새 레벨·게시판 화면은 Notion을 사용하지 않습니다.
